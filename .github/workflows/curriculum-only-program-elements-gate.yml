name: Curriculum-Only Program Elements Gate

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary validation workflow retired; safe to delete.'
